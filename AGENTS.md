# Codex Instructions

## VSCode MCP

- For every `vscode-mcp` tool that requires `workspace_path`, use the exact path `d:\PythonStudentWork\PythonProjects\PythonProject4`.
- Keep the drive letter lowercase. On Windows, VSCode MCP Bridge hashes the workspace path case-sensitively when selecting its named pipe.
